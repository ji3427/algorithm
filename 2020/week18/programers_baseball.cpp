#include <iostream>
#include <string>
#include <vector>
using namespace std;

bool isans[1000];

int vec_to_int(vector<int> num){
	int n = 
}

vector<int> judge(vector<int> pivot, vector<int> num){
	vector<int> ans;
	int s = 0;
	int b = 0;
	for(int i = 0 ; i < 3 ; i++){
		if(pivot[i] == num[i]).
	}
}


int solution(vector<vector<int>> baseball) {
    int answer = 0;
    vector<int> num(3);
    for(int i = 1 ; i <= 9 ; i++){
		num[0] = i''
		for(int j = 1 ; j <= 9 ; j++){
			num[1] = j
			for(int k = 1 ; k <= 9 ; k++){
				num[2] = k;
				if(num[0] == num[1] || num[0] == num[2] || num[1] == num[2]) continue;
			}
		}
	}
    return answer;
}


int main(void){
	
}
